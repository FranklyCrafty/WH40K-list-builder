// models/costModel.js

  class Cost {
    constructor(costData) {
      this.id = costData.id;
      this.points = costData.points;
      this.modifier_condition = costData.modifier_condition;    // condition type attribute
      this.modifier_target = costData.modifier_target;          // childID
      this.modifier_points = costData.modifier_points;          // points if modifier condition is true
    }
  }
  
  module.exports = Cost;